{
  "name": "checkout-platform",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "setup": "cp apps/api/.env.example apps/api/.env && cp apps/web/.env.local.example apps/web/.env.local",
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "@nestjs/core"
    ]
  }
}
